const width = 16;
const length = 20;

const perimeter = function (width, length) {
  a = width * length;
  console.log(`Width: ${width}`);
  console.log(`length: ${length}`);
  console.log(`Area: ${a}`);
};

perimeter(width, length);
